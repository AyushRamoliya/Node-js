<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<style>
    /* General styling for body */
body {
    font-family: 'Roboto', Arial, sans-serif;
    background: linear-gradient(135deg, #f9f9f9, #e6f2ff);
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    color: #333;
}

/* Styling for the form container */
div {
    background-color: #ffffff;
    border: 2px solid #007BFF;
    border-radius: 12px;
    padding: 30px 40px;
    box-shadow: 0 8px 15px rgba(0, 0, 0, 0.15);
    width: 320px;
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

div:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
}

/* Form header styling */
h1 {
    color: #0056b3;
    font-size: 26px;
    margin-bottom: 25px;
}

/* Input fields styling */
input[type="text"], input[type="number"], input[type="file"] {
    width: 100%;
    padding: 12px;
    margin: 12px 0;
    border: 1px solid #ccc;
    border-radius: 6px;
    font-size: 15px;
    box-sizing: border-box;
    background-color: #f9f9f9;
    transition: border-color 0.3s ease, box-shadow 0.3s ease, background-color 0.3s ease;
}

/* Hover and focus effects for input fields */
input[type="text"]:hover, 
input[type="number"]:hover, 
input[type="file"]:hover {
    border-color: #0056b3;
    background-color: #e6f2ff;
}

input[type="text"]:focus, 
input[type="number"]:focus, 
input[type="file"]:focus {
    border-color: #007BFF;
    box-shadow: 0 0 8px rgba(0, 123, 255, 0.6);
    outline: none;
    background-color: #e6f7ff;
}

/* Button styling */
button {
    width: 100%;
    background: linear-gradient(135deg, #007BFF, #0056b3);
    color: white;
    padding: 14px 20px;
    border: none;
    border-radius: 6px;
    font-size: 17px;
    cursor: pointer;
    font-weight: bold;
    transition: background 0.3s ease, transform 0.2s ease, box-shadow 0.3s ease;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

/* Button hover effect */
button:hover {
    background: linear-gradient(135deg, #0056b3, #003f8a);
    transform: scale(1.08);
    box-shadow: 0 6px 10px rgba(0, 0, 0, 0.2);
}

/* Button active effect */
button:active {
    transform: scale(1);
    box-shadow: 0 3px 5px rgba(0, 0, 0, 0.1);
    background: linear-gradient(135deg, #004080, #002653);
}

</style>
<body>
    <div >
        <form action="/addData" enctype="multipart/form-data"  method="post" >
            
            <h1 style="font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;">Books Cart</h1>
       
            <input type="text" placeholder="Enter Movie Name" name="name" required >
            <input type="text" placeholder="Enter Movie Charecter" name="auther" required>
            <input type="text" placeholder="Enter Movie Director" name="prize" required >
            <input type="file" name="image" required ><br>
            <button type="submit">Add Data</button>
        </form>
    </div>

   
</body>
</html>